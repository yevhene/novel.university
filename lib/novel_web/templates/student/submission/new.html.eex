<h2><%= gettext "New Submission for Lab \"%{lab}\"", lab: @lab.title %></h2>


<%= render "_form.html", Map.put(assigns, :action, student_course_lab_submission_path(@conn, :create, @course, @lab)) %>

<hr />
<span><%= link gettext("Back"), to: student_course_lab_path(@conn, :show, @course, @lab) %></span>
