<li class="list-group-item">
  <div class="d-flex align-items-center justify-content-between">
    <%= link to: student_course_quiz_path(@conn, :show, @course, @quiz) do %>
      <span><%= @quiz.name %></span>
    <% end %>

    <%= status_icon List.first(@quiz.results).is_passed %>
  </div>
</li>
