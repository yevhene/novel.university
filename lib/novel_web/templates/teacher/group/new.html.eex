<%= render "_form.html", Map.put(assigns, :action, teacher_course_group_path(@conn, :create, @course)) %>

<hr />
<span><%= link gettext("Back"), to: teacher_course_group_path(@conn, :index, @course) %></span>
