<%= render "_form.html", Map.put(assigns, :action, teacher_course_quiz_question_path(@conn, :create, @course, @quiz)) %>

<hr />
<span><%= link gettext("Back"), to: teacher_course_quiz_path(@conn, :show, @course, @quiz) %></span>
