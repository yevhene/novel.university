<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :is_approved, class: "control-label" %>
    <%= select f, :is_approved, ["Approved": true, "Rejected": false, "Not Approved Yet": nil], class: "form-control" %>
    <%= error_tag f, :is_approved %>
  </div>

  <div class="form-group">
    <%= label f, :group_id, class: "control-label" %>
    <%= select f, :group_id, enrollment_group_options(@groups), class: "form-control" %>
    <%= error_tag f, :group_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
