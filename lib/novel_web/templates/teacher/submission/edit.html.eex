<%= render "_form.html", Map.put(assigns, :action, teacher_course_submission_path(@conn, :update, @course, @submission)) %>

<hr />
<span><%= link gettext("Back"), to: teacher_course_submission_path(@conn, :show, @course, @submission) %></span>
