<h2><%= gettext "Link repository" %></h2>

<%= render "_form.html", Map.put(assigns, :action, teacher_course_repository_path(@conn, :update, @course)) %>

<hr />
<span><%= link gettext("Back"), to: teacher_course_repository_path(@conn, :show, @course) %></span>
