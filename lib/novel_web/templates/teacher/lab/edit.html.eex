<%= render "_form.html", Map.put(assigns, :action, teacher_course_lab_path(@conn, :update, @course, @lab)) %>

<span><%= link gettext("Back"), to: teacher_course_lab_path(@conn, :index, @course) %></span>
